{
  "name": "cursor-to-jira-integration",
  "version": "2.0.0",
  "type": "module",
  "bin": {
    "jira-cli": "./bin/jira-cli.mjs"
  },
  "scripts": {
    "start": "node bin/jira-cli.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "lint": "npx eslint lib/ commands/ bin/"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=18.3.0"
  }
}
